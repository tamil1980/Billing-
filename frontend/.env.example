# Leave empty in production: the API and the frontend are served from the same URL.
# For local development (Vite on :5173, API on :5000), copy to .env.local and set:
VITE_API_BASE_URL=http://localhost:5000